<h2>Wubba</h2>

<!-- you could add a class to these p's and add font-weight rather than using strong? -->
<p>
  <strong>Question:</strong>
  <%= @question.body%>
</p>
<p><strong>Color:</strong>
<div class="color-field" style="background-color: <%= @question.color %>">
  <%= @question.color %>
</div>
</p>

<%= render partial: "response/form" %>

<%= link_to 'Back', questions_path %>
